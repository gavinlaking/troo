#!/usr/bin/env ruby

trap("SIGINT") { exit! }

require_relative "../lib/troo"

begin
  puts
  Troo::CLI::Main.start(ARGV)
rescue Troo::InvalidAccessToken
  puts "Your Trello access credentials have expired, please renew and try again."
rescue SocketError
  puts "Cannot continue, no network connection."
end
